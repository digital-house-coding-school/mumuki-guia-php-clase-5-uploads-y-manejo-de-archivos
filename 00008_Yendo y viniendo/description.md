Esta función se pone un poco más compleja.

La función `agregarUsuario` recibe 2 parámetros

1. Un **string** en formato JSON que tiene ya tiene toda la base de usuarios
2. Un **array** con los datos de un usuario nuevo a agregar en la base de usuarios.


Tu objetivo es retornar un **string en formato JSON** que tenga toda la base anterior y el usuario nuevo agregado.

¿No sabes como encarar el ejercicio? Te dejamos una pista acá abajo :point_down:
